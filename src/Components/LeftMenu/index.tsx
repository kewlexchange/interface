import {
  Avatar,
  AvatarGroup,
  Button,
  Card,
  CardBody,
  Drawer,
  DrawerBody,
  DrawerContent,
  DrawerFooter,
  DrawerHeader,
  Image,
  Link,
  ScrollShadow,
  Tab,
  Tabs,
  Tooltip,
} from '@nextui-org/react';
import { useState } from 'react';

import { CopyX } from 'lucide-react';

 

export const LeftMenu = ({ isOpen, onClose }) => {
  const [selected, setSelected] = useState<string>("pins");

  return (
<div>
    asdada
</div>
  );
};
